import React from "react";
import { configure } from "__mocks__/enzyme";

test("calls configure", () => {
  // configure.mockImplementationOnce(() => (void));
  // expect(configure).toHaveBeenCalled();
});
